import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Subscription details",
};

const Page = () => {
  return <main className="my-10">This is biiling page</main>;
};

export default Page;
