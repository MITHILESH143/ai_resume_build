import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Resumes",
};

const Page = () => {
  return <div>This is page where you can see your all resumes</div>;
};

export default Page;
